# Frontend_Multiframework_conversion-
A full-stack web app that converts HTML → React JSX and CSS → Tailwind CSS. Built with React + Vite (frontend) and Python Flask + MySQL (backend). Supports single file conversion, merged output, and bulk ZIP upload with auto-download. Includes conversion history stored in MySQL.
